@page
@model IndexModel
@{
    ViewData["Title"] = "Bem-vindo ao Vigia";
}

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />

<div class="container mt-5">
    <div class="row">
        <div class="col-md-7">
            <h1>Bem-vindo ao Vigia</h1>
            <p>Controle fácil e seguro da validade dos seus documentos.</p>
            <ul>
                <li>Acompanhe documentos próximos do vencimento</li>
                <li>Receba alertas automáticos</li>
                <li>Gerencie tudo em um só lugar</li>
            </ul>
        </div>
        <div class="col-md-5">
            <ul class="nav nav-tabs" id="authTab" role="tablist">
                <li class="nav-item" role="presentation">
                    <button class="nav-link active" id="login-tab" data-bs-toggle="tab" data-bs-target="#login"
                        type="button" role="tab" aria-controls="login" aria-selected="true">Login</button>
                </li>
                <li class="nav-item" role="presentation">
                    <button class="nav-link" id="register-tab" data-bs-toggle="tab" data-bs-target="#register"
                        type="button" role="tab" aria-controls="register" aria-selected="false">Registrar</button>
                </li>
            </ul>
            <div class="tab-content border border-top-0 p-3" id="authTabContent">
                <!-- Formulário de Login -->
                <div class="tab-pane fade show active" id="login" role="tabpanel" aria-labelledby="login-tab">
                    <form method="post" asp-area="Identity" asp-page="/Account/Login" asp-route-returnUrl="/">
                        <div class="mb-3">
                            <label for="Input_Email" class="form-label">Email</label>
                            <input type="email" class="form-control" id="Input_Email" name="Input.Email" required />
                        </div>
                        <div class="mb-3">
                            <label for="Input_Password" class="form-label">Senha</label>
                            <input type="password" class="form-control" id="Input_Password" name="Input.Password"
                                required />
                        </div>
                        <div class="mb-3 form-check">
                            <input type="checkbox" class="form-check-input" id="Input_RememberMe"
                                name="Input.RememberMe" />
                            <label class="form-check-label" for="Input_RememberMe">Lembrar-me</label>
                        </div>
                        <button type="submit" class="btn btn-primary w-100">Entrar</button>
                    </form>
                </div>

                <!-- Formulário de Registro -->
                <div class="tab-pane fade" id="register" role="tabpanel" aria-labelledby="register-tab">
                    <form method="post" asp-area="Identity" asp-page="/Account/Register" asp-route-returnUrl="/">
                        <div class="mb-3">
                            <label for="Input_RegisterEmail" class="form-label">Email</label>
                            <input type="email" class="form-control" id="Input_RegisterEmail" name="Input.Email"
                                required />
                        </div>
                        <div class="mb-3">
                            <label for="Input_RegisterPassword" class="form-label">Senha</label>
                            <input type="password" class="form-control" id="Input_RegisterPassword"
                                name="Input.Password" required />
                        </div>
                        <div class="mb-3">
                            <label for="Input_RegisterConfirmPassword" class="form-label">Confirmar Senha</label>
                            <input type="password" class="form-control" id="Input_RegisterConfirmPassword"
                                name="Input.ConfirmPassword" required />
                        </div>
                        <button type="submit" class="btn btn-success w-100">Registrar</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
